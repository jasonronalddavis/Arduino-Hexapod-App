

#include <SPI.h>
#include <Arduino.h>
#include <ESP32Servo.h>

// Define the eye servos
Servo eyeX;
Servo eyeY;
Servo eyelids;

// Initialize the eye servos
void initEyes() {
  // eyeX.attach(41);
  // eyeY.attach(44);
  // eyelids.attach(45);
}

// Control eye and eyelid movements
void eyeFunction() {
  // // Eye movements
  // eyeX.write(80);
  // delay(900);
  // eyeX.write(130);
  // delay(900);
  // eyeX.write(80);
  // delay(500);
  // eyeX.write(130);

  // eyeY.write(20);
  // delay(500);
  // eyeY.write(60);
  // delay(500);
  // eyeY.write(20);
  // delay(500);
  // eyeY.write(60);
  // delay(900);

  // // Eyelid movements
  // eyelids.write(75);
  // delay(250);
  // eyelids.write(140);
  // delay(250);
  // eyelids.write(75);
  // delay(250);
  // eyelids.write(140);
  // delay(250);
}

